# Base URLs
rest.base.url=https://api.volunteer-system.com/v1
websocket.base.url=wss://api.volunteer-system.com/ws

# Timeouts
connection.timeout=5000
read.timeout=10000

# Retry policy
max.retries=3
retry.delay=1000